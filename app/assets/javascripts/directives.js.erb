(function() {

  var app = angular.module('directives', []);

  app.directive('projects', function() {
    return {
      restrict: 'E',
      templateUrl: '<%= asset_url("templates/projects.html.haml") %>'
    };
  });

  app.directive('tasks', function() {
    return {
      restrict: 'E',
      templateUrl: '<%= asset_url("templates/tasks.html.haml") %>'
    };
  });

})();